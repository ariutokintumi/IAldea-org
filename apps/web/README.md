# Web apps

- **configurator/** — static MVP (Day 1–2): open `configurator/index.html` in a browser to generate `SOUL.md` and `policy_config.yaml`.

Additional surfaces (Day 4+) may live alongside per the main README.
